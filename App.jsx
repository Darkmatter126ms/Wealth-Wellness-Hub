import { useState, useEffect, useCallback, useRef } from "react"; import { Shield, Lock, Eye, EyeOff, ChevronRight, ChevronDown, ChevronUp, TrendingUp, TrendingDown, Wallet, PieChart, BarChart3, Brain, Settings, Bell, LogOut, Plus, ArrowRight, ArrowUpRight, ArrowDownRight, CheckCircle, AlertTriangle, Info, Star, Zap, Crown, CreditCard, Globe, Building2, Bitcoin, Landmark, Car, Home, Gem, DollarSign, Target, Activity, ShieldCheck, Sparkles, X, Menu, User, HelpCircle, RefreshCw, Download, Filter, Search, Smartphone, Key, ChevronLeft, FileText, MessageSquare, Lightbulb, AlertCircle, Clock, Trash2 } from "lucide-react"; import { PieChart as RPieChart, Pie, Cell, ResponsiveContainer, AreaChart, Area, XAxis, YAxis, Tooltip, CartesianGrid, BarChart, Bar, RadarChart, Radar, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Legend } from "recharts"; /* ─────────── API HELPER ─────────── */ const api = { token: null, async request(method, path, body = null) { const opts = { method, headers: { 'Content-Type': 'application/json' }, }; if (this.token) opts.headers['Authorization'] = `Bearer ${this.token}`; if (body) opts.body = JSON.stringify(body); const res = await fetch(`/api${path}`, opts); const data = await res.json(); if (!res.ok) throw new Error(data.error || 'Request failed'); return data; }, login(email, password) { return this.request('POST', '/auth/login', { email, password }); }, verify2FA(tempToken, code) { return this.request('POST', '/auth/verify-2fa', { tempToken, code }); }, logout() { return this.request('POST', '/auth/logout'); }, getPortfolio() { return this.request('GET', '/portfolio'); }, addAsset(asset) { return this.request('POST', '/portfolio/assets', asset); }, deleteAsset(id) { return this.request('DELETE', `/portfolio/assets/${id}`); }, getWellness() { return this.request('GET', '/wellness'); }, getInsights() { return this.request('GET', '/insights'); }, chat(message) { return this.request('POST', '/ai/chat', { message }); }, simulate(params) { return this.request('POST', '/scenarios/simulate', params); }, createCheckout(plan) { return this.request('POST', '/payments/create-checkout', { plan }); }, getSubscription() { return this.request('GET', '/payments/subscription'); }, getProfile() { return this.request('GET', '/user/profile'); }, updateProfile(data) { return this.request('PUT', '/user/profile', data); },
  getGoals() { return this.request('GET', '/goals'); },
  createGoal(goal) { return this.request('POST', '/goals', goal); },
  updateGoal(id, data) { return this.request('PUT', '/goals/' + id, data); },
  deleteGoal(id) { return this.request('DELETE', '/goals/' + id); },
  getEmailPrefs() { return this.request('GET', '/email-preferences'); },
  updateEmailPrefs(data) { return this.request('PUT', '/email-preferences', data); },
  sendReport(email) { return this.request('POST', '/email-preferences/send-now', { email }); }, }; /* ─────────── FONTS + STYLES ─────────── */ const FONTS_LINK = "https://fonts.googleapis.com/css2?family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,600;0,9..40,700;1,9..40,400&family=Fraunces:opsz,wght@9..144,300;9..144,400;9..144,500;9..144,600;9..144,700&display=swap"; const CSS = ` @keyframes fadeIn { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } } @keyframes slideIn { from { opacity: 0; transform: translateX(-20px); } to { opacity: 1; transform: translateX(0); } } @keyframes pulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.6; } } @keyframes scaleIn { from { opacity: 0; transform: scale(0.95); } to { opacity: 1; transform: scale(1); } } @keyframes spinSlow { from { transform: rotate(0deg); } to { transform: rotate(360deg); } } @keyframes gradientShift { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } } * { box-sizing: border-box; margin: 0; padding: 0; } :root { --bg-primary: #0B1120; --bg-secondary: #111827; --bg-card: #1A2332; --bg-card-hover: #1E293B; --bg-elevated: #243044; --text-primary: #F1F5F9; --text-secondary: #94A3B8; --text-muted: #64748B; --accent-teal: #0EA5E9; --accent-gold: #F59E0B; --accent-green: #10B981; --accent-red: #EF4444; --accent-purple: #8B5CF6; --border: rgba(148, 163, 184, 0.1); --border-hover: rgba(148, 163, 184, 0.2); --shadow: 0 4px 24px rgba(0,0,0,0.3); --radius: 16px; --radius-sm: 10px; --radius-xs: 6px; } body { font-family: 'DM Sans', sans-serif; background: var(--bg-primary); color: var(--text-primary); -webkit-font-smoothing: antialiased; } .font-display { font-family: 'Fraunces', serif; } .card { background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius); transition: all 0.25s ease; } .card:hover { border-color: var(--border-hover); box-shadow: var(--shadow); } .btn-primary { background: linear-gradient(135deg, #0EA5E9, #6366F1); color: white; border: none; padding: 12px 28px; border-radius: var(--radius-sm); font-weight: 600; font-size: 15px; cursor: pointer; transition: all 0.2s ease; font-family: 'DM Sans', sans-serif; } .btn-primary:hover { transform: translateY(-1px); box-shadow: 0 4px 20px rgba(14,165,233,0.3); } .btn-primary:disabled { opacity: 0.5; cursor: not-allowed; transform: none; } .btn-secondary { background: var(--bg-elevated); color: var(--text-primary); border: 1px solid var(--border); padding: 10px 24px; border-radius: var(--radius-sm); font-weight: 500; font-size: 14px; cursor: pointer; transition: all 0.2s ease; font-family: 'DM Sans', sans-serif; } .btn-secondary:hover { border-color: var(--border-hover); background: var(--bg-card-hover); } .btn-ghost { background: transparent; color: var(--text-secondary); border: none; padding: 8px 16px; border-radius: var(--radius-xs); cursor: pointer; transition: all 0.15s ease; font-family: 'DM Sans', sans-serif; font-size: 14px; } .btn-ghost:hover { color: var(--text-primary); background: rgba(255,255,255,0.05); } .input { background: var(--bg-secondary); border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 12px 16px; color: var(--text-primary); font-size: 15px; font-family: 'DM Sans', sans-serif; width: 100%; outline: none; transition: border-color 0.2s; } .input:focus { border-color: var(--accent-teal); } .input::placeholder { color: var(--text-muted); } .badge { display: inline-flex; align-items: center; padding: 4px 10px; border-radius: 20px; font-size: 12px; font-weight: 600; gap: 4px; } .progress-ring { transform: rotate(-90deg); } .animate-in { animation: fadeIn 0.5s ease forwards; } .animate-in-delay-1 { animation: fadeIn 0.5s ease 0.1s forwards; opacity: 0; } .animate-in-delay-2 { animation: fadeIn 0.5s ease 0.2s forwards; opacity: 0; } .animate-in-delay-3 { animation: fadeIn 0.5s ease 0.3s forwards; opacity: 0; } .sidebar-item { display: flex; align-items: center; gap: 12px; padding: 12px 16px; border-radius: var(--radius-sm); cursor: pointer; transition: all 0.2s; color: var(--text-secondary); font-size: 14px; font-weight: 500; border: none; background: none; width: 100%; text-align: left; font-family: 'DM Sans', sans-serif; } .sidebar-item:hover { background: rgba(255,255,255,0.05); color: var(--text-primary); } .sidebar-item.active { background: rgba(14,165,233,0.12); color: var(--accent-teal); } .tooltip-custom { background: var(--bg-elevated) !important; border: 1px solid var(--border) !important; border-radius: var(--radius-xs) !important; color: var(--text-primary) !important; font-family: 'DM Sans', sans-serif !important; box-shadow: var(--shadow) !important; } .scrollbar-thin::-webkit-scrollbar { width: 4px; } .scrollbar-thin::-webkit-scrollbar-track { background: transparent; } .scrollbar-thin::-webkit-scrollbar-thumb { background: var(--border-hover); border-radius: 2px; } .tab-btn { padding: 8px 20px; border-radius: 8px; font-size: 13px; font-weight: 500; cursor: pointer; transition: all 0.2s; border: none; font-family: 'DM Sans', sans-serif; background: transparent; color: var(--text-muted); } .tab-btn:hover { color: var(--text-secondary); } .tab-btn.active { background: var(--accent-teal); color: white; } .insight-card { border-left: 3px solid; padding: 16px 20px; border-radius: 0 var(--radius-sm) var(--radius-sm) 0; background: var(--bg-card); transition: all 0.2s; cursor: pointer; } .insight-card:hover { background: var(--bg-card-hover); } .insight-card.warning { border-left-color: var(--accent-gold); } .insight-card.opportunity { border-left-color: var(--accent-teal); } .insight-card.positive { border-left-color: var(--accent-green); } .tier-card { border: 2px solid var(--border); border-radius: var(--radius); padding: 32px 24px; transition: all 0.3s; position: relative; overflow: hidden; } .tier-card.recommended { border-color: var(--accent-teal); background: linear-gradient(180deg, rgba(14,165,233,0.08) 0%, transparent 40%); } .tier-card:hover { transform: translateY(-4px); box-shadow: var(--shadow); } .mesh-gradient { background: radial-gradient(at 20% 80%, rgba(14,165,233,0.15) 0%, transparent 50%), radial-gradient(at 80% 20%, rgba(139,92,246,0.12) 0%, transparent 50%), radial-gradient(at 50% 50%, rgba(16,185,129,0.08) 0%, transparent 50%); } .error-msg { color: var(--accent-red); font-size: 13px; margin-top: 8px; display: flex; align-items: center; gap: 6px; } .loading-spinner { animation: spinSlow 1s linear infinite; } `; /* ─────────── UTILITY COMPONENTS ─────────── */ const formatCurrency = (v) => v >= 1000000 ? `$${(v/1000000).toFixed(2)}M` : v >= 1000 ? `$${(v/1000).toFixed(1)}K` : `$${v.toFixed(0)}`; const formatFullCurrency = (v) => `$${Math.round(v).toLocaleString("en-SG")}`; const WealthScore = ({ score, size = 160 }) => { const sw = 10, r = (size - sw) / 2, c = 2 * Math.PI * r; const color = score >= 75 ? "#10B981" : score >= 50 ? "#F59E0B" : "#EF4444"; return ( <div style={{ position: "relative", width: size, height: size, display: "flex", alignItems: "center", justifyContent: "center" }}> <svg width={size} height={size} className="progress-ring"> <circle cx={size/2} cy={size/2} r={r} fill="none" stroke="rgba(148,163,184,0.1)" strokeWidth={sw} /> <circle cx={size/2} cy={size/2} r={r} fill="none" stroke={color} strokeWidth={sw} strokeDasharray={c} strokeDashoffset={c - (score/100)*c} strokeLinecap="round" style={{ transition: "stroke-dashoffset 1.5s ease" }} /> </svg> <div style={{ position: "absolute", textAlign: "center" }}> <div className="font-display" style={{ fontSize: size*0.28, fontWeight: 700, color }}>{score}</div> <div style={{ fontSize: 11, color: "var(--text-muted)", fontWeight: 500, marginTop: -2 }}>Wellness Score</div> </div> </div> ); }; const AssetIcon = ({ type, size = 18 }) => { const p = { size, strokeWidth: 1.8 }; const icons = { bank: Landmark, landmark: Landmark, trending: TrendingUp, building: Building2, bitcoin: Bitcoin, shield: ShieldCheck, home: Home, car: Car }; const Icon = icons[type] || DollarSign; return <Icon {...p} />; }; const CustomTooltip = ({ active, payload, label }) => { if (!active || !payload?.length) return null; return ( <div className="tooltip-custom" style={{ padding: "10px 14px" }}> <div style={{ fontSize: 12, color: "var(--text-muted)", marginBottom: 4 }}>{label}</div> {payload.map((p, i) => ( <div key={i} style={{ fontSize: 14, fontWeight: 600, color: p.color || "var(--text-primary)" }}>{formatFullCurrency(p.value)}</div> ))} </div> ); }; const Loader = ({ text = "Loading..." }) => ( <div style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", padding: 60, gap: 12 }}> <RefreshCw size={24} color="var(--accent-teal)" className="loading-spinner" /> <span style={{ color: "var(--text-muted)", fontSize: 14 }}>{text}</span> </div> ); /* ─────────── LOGIN PAGE ─────────── */ const LoginPage = ({ onLogin }) => { const [step, setStep] = useState("login"); const [email, setEmail] = useState("sarah@wealthwell.com"); const [password, setPassword] = useState("demo1234"); const [showPass, setShowPass] = useState(false); const [code, setCode] = useState(""); const [loading, setLoading] = useState(false); const [error, setError] = useState(""); const [tempToken, setTempToken] = useState(""); const handleLogin = async () => { if (!email || !password) return; setLoading(true); setError(""); try { const data = await api.login(email, password); if (data.requires2FA) { setTempToken(data.tempToken); setStep("2fa"); } else { api.token = data.token; onLogin(data.user); } } catch (err) { setError(err.message); } finally { setLoading(false); } }; const handleVerify = async () => { if (code.length < 6) return; setLoading(true); setError(""); try { const data = await api.verify2FA(tempToken, code); api.token = data.token; onLogin(data.user); } catch (err) { setError(err.message); } finally { setLoading(false); } }; return ( <div style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", padding: 20 }} className="mesh-gradient"> <div className="animate-in" style={{ width: "100%", maxWidth: 420 }}> <div style={{ textAlign: "center", marginBottom: 40 }}> <div style={{ display: "inline-flex", alignItems: "center", gap: 12, marginBottom: 8 }}> <div style={{ width: 48, height: 48, borderRadius: 14, background: "linear-gradient(135deg, #0EA5E9, #6366F1)", display: "flex", alignItems: "center", justifyContent: "center" }}> <Wallet size={26} color="white" strokeWidth={1.8} /> </div> <span className="font-display" style={{ fontSize: 28, fontWeight: 600 }}>WealthWell</span> </div> <p style={{ color: "var(--text-muted)", fontSize: 14, marginTop: 4 }}>Your financial wellness, unified.</p> </div> <div className="card" style={{ padding: 32 }}> {step === "login" ? ( <> <h2 style={{ fontSize: 20, fontWeight: 600, marginBottom: 4 }}>Welcome back</h2> <p style={{ color: "var(--text-muted)", fontSize: 14, marginBottom: 24 }}>Sign in to your Wealth Wallet</p> <div style={{ marginBottom: 16 }}> <label style={{ fontSize: 13, color: "var(--text-secondary)", fontWeight: 500, display: "block", marginBottom: 6 }}>Email</label> <input className="input" type="email" value={email} onChange={e => setEmail(e.target.value)} placeholder="you@email.com" /> </div> <div style={{ marginBottom: 24 }}> <label style={{ fontSize: 13, color: "var(--text-secondary)", fontWeight: 500, display: "block", marginBottom: 6 }}>Password</label> <div style={{ position: "relative" }}> <input className="input" type={showPass ? "text" : "password"} value={password} onChange={e => setPassword(e.target.value)} placeholder="Enter your password" style={{ paddingRight: 44 }} onKeyDown={e => e.key === "Enter" && handleLogin()} /> <button onClick={() => setShowPass(!showPass)} style={{ position: "absolute", right: 12, top: "50%", transform: "translateY(-50%)", background: "none", border: "none", cursor: "pointer", color: "var(--text-muted)", display: "flex" }}> {showPass ? <EyeOff size={18} /> : <Eye size={18} />} </button> </div> </div> {error && <div className="error-msg"><AlertCircle size={14} /> {error}</div>} <button className="btn-primary" style={{ width: "100%", display: "flex", alignItems: "center", justifyContent: "center", gap: 8, marginTop: error ? 12 : 0 }} onClick={handleLogin} disabled={loading}> {loading ? <RefreshCw size={18} className="loading-spinner" /> : <><Lock size={16} /> Sign In</>} </button> <div style={{ display: "flex", alignItems: "center", gap: 12, margin: "20px 0", color: "var(--text-muted)" }}> <div style={{ flex: 1, height: 1, background: "var(--border)" }} /><span style={{ fontSize: 12 }}>or</span><div style={{ flex: 1, height: 1, background: "var(--border)" }} /> </div> <button className="btn-secondary" style={{ width: "100%", display: "flex", alignItems: "center", justifyContent: "center", gap: 8, fontSize: 14 }}> <Smartphone size={16} /> Sign in with SingPass </button> <p style={{ textAlign: "center", fontSize: 13, color: "var(--text-muted)", marginTop: 20 }}> Don't have an account? <span style={{ color: "var(--accent-teal)", cursor: "pointer", fontWeight: 500 }}>Get started free</span> </p> </> ) : ( <div className="animate-in"> <div style={{ textAlign: "center", marginBottom: 24 }}> <div style={{ width: 56, height: 56, borderRadius: 16, margin: "0 auto 16px", background: "rgba(14,165,233,0.12)", display: "flex", alignItems: "center", justifyContent: "center" }}> <ShieldCheck size={28} color="var(--accent-teal)" /> </div> <h2 style={{ fontSize: 20, fontWeight: 600, marginBottom: 4 }}>Two-Factor Authentication</h2> <p style={{ color: "var(--text-muted)", fontSize: 14 }}>Enter the 6-digit code from your authenticator app</p> </div> <div style={{ marginBottom: 24 }}> <input className="input" value={code} onChange={e => setCode(e.target.value.replace(/\D/g, "").slice(0, 6))} placeholder="000000" maxLength={6} onKeyDown={e => e.key === "Enter" && handleVerify()} style={{ textAlign: "center", fontSize: 28, letterSpacing: 12, fontWeight: 600 }} /> </div> {error && <div className="error-msg" style={{ marginBottom: 12 }}><AlertCircle size={14} /> {error}</div>} <button className="btn-primary" style={{ width: "100%", display: "flex", alignItems: "center", justifyContent: "center", gap: 8 }} onClick={handleVerify} disabled={loading || code.length < 6}> {loading ? <RefreshCw size={18} className="loading-spinner" /> : <><ShieldCheck size={16} /> Verify & Continue</>} </button> <button className="btn-ghost" style={{ width: "100%", marginTop: 12, fontSize: 13 }} onClick={() => { setStep("login"); setError(""); }}> <ChevronLeft size={14} style={{ marginRight: 4 }} /> Back to login </button> </div> )} </div> <div style={{ display: "flex", justifyContent: "center", gap: 24, marginTop: 24 }}> {[{ icon: <Lock size={14} />, text: "Bank-grade encryption" }, { icon: <ShieldCheck size={14} />, text: "MAS regulated" }, { icon: <Eye size={14} />, text: "Read-only access" }].map((t, i) => ( <div key={i} style={{ display: "flex", alignItems: "center", gap: 6, fontSize: 12, color: "var(--text-muted)" }}>{t.icon} {t.text}</div> ))} </div> </div> </div> ); }; /* ─────────── ONBOARDING ─────────── */ const OnboardingPage = ({ onComplete }) => { const [step, setStep] = useState(0); const steps = [ { icon: <Wallet size={32} color="var(--accent-teal)" />, title: "Welcome to WealthWell", desc: "We'll help you see your complete financial picture in one place. No more switching between apps." }, { icon: <Shield size={32} color="var(--accent-green)" />, title: "Your Privacy, Our Priority", desc: "We use read-only connections. We can never move your money. All data is encrypted end-to-end with AES-256." }, { icon: <Brain size={32} color="var(--accent-purple)" />, title: "AI-Powered Insights", desc: "Our AI coach analyzes your portfolio and gives you plain-English recommendations — no jargon, just clear next steps." }, { icon: <Target size={32} color="var(--accent-gold)" />, title: "Connect Your Accounts", desc: "Link your bank accounts, brokerages, CPF, and crypto wallets. The more you connect, the better your wellness score." }, ]; return ( <div style={{ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", padding: 20 }} className="mesh-gradient"> <div className="animate-in" style={{ width: "100%", maxWidth: 480 }}> <div className="card" style={{ padding: 40, textAlign: "center" }}> <div style={{ display: "flex", gap: 6, justifyContent: "center", marginBottom: 36 }}> {steps.map((_, i) => ( <div key={i} style={{ width: i === step ? 32 : 8, height: 8, borderRadius: 4, background: i <= step ? "var(--accent-teal)" : "var(--border)", transition: "all 0.3s ease" }} /> ))} </div> <div key={step} className="animate-in"> <div style={{ width: 72, height: 72, borderRadius: 20, margin: "0 auto 24px", background: "rgba(255,255,255,0.03)", border: "1px solid var(--border)", display: "flex", alignItems: "center", justifyContent: "center" }}> {steps[step].icon} </div> <h2 className="font-display" style={{ fontSize: 24, fontWeight: 600, marginBottom: 12 }}>{steps[step].title}</h2> <p style={{ color: "var(--text-secondary)", fontSize: 15, lineHeight: 1.6, maxWidth: 380, margin: "0 auto" }}>{steps[step].desc}</p> </div> {step === 3 && ( <div className="animate-in" style={{ marginTop: 24, display: "flex", flexDirection: "column", gap: 10 }}> {[{ name: "Bank Accounts (DBS, OCBC, UOB)", connected: true }, { name: "CPF & SRS", connected: true }, { name: "Brokerage (FSMOne, IBKR)", connected: true }, { name: "Robo-Advisors (Syfe, Endowus)", connected: true }, { name: "Crypto (Coinhako)", connected: false }].map((acc, i) => ( <div key={i} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "12px 16px", borderRadius: "var(--radius-sm)", background: "var(--bg-secondary)", border: "1px solid var(--border)", textAlign: "left" }}> <span style={{ fontSize: 14 }}>{acc.name}</span> {acc.connected ? ( <span style={{ display: "flex", alignItems: "center", gap: 4, color: "var(--accent-green)", fontSize: 13, fontWeight: 500 }}><CheckCircle size={14} /> Connected</span> ) : ( <button className="btn-ghost" style={{ fontSize: 13, color: "var(--accent-teal)", fontWeight: 500, padding: "4px 12px" }}><Plus size={14} style={{ marginRight: 4 }} /> Connect</button> )} </div> ))} </div> )} <div style={{ display: "flex", gap: 12, justifyContent: "center", marginTop: 32 }}> {step > 0 && <button className="btn-secondary" onClick={() => setStep(step - 1)}>Back</button>} <button className="btn-primary" onClick={() => step < steps.length - 1 ? setStep(step + 1) : onComplete()} style={{ display: "flex", alignItems: "center", gap: 8 }}> {step === steps.length - 1 ? "Launch Dashboard" : "Continue"} <ChevronRight size={16} /> </button> </div> </div> </div> </div> ); }; /* ─────────── DASHBOARD VIEW ─────────── */ const DashboardView = ({ portfolio, wellness, insights, onNavigate }) => { if (!portfolio || !wellness) return <Loader text="Loading dashboard..." />; return ( <div style={{ display: "flex", flexDirection: "column", gap: 20 }}> <div className="animate-in" style={{ display: "grid", gridTemplateColumns: "1fr 200px", gap: 20 }}> <div className="card" style={{ padding: 28, position: "relative", overflow: "hidden" }}> <div style={{ position: "absolute", top: 0, right: 0, width: 200, height: 200, background: "radial-gradient(circle, rgba(14,165,233,0.08), transparent 70%)", borderRadius: "0 0 0 100%" }} /> <div style={{ fontSize: 14, color: "var(--text-muted)", marginBottom: 4 }}>Good morning,</div> <h1 className="font-display" style={{ fontSize: 28, fontWeight: 600, marginBottom: 16 }}>Sarah <span style={{ fontSize: 20 }}>👋</span></h1> <div style={{ display: "flex", gap: 40, flexWrap: "wrap" }}> <div> <div style={{ fontSize: 12, color: "var(--text-muted)", fontWeight: 500, marginBottom: 4, textTransform: "uppercase", letterSpacing: 1 }}>Total Net Worth</div> <div className="font-display" style={{ fontSize: 32, fontWeight: 700 }}>{formatFullCurrency(portfolio.totalWealth)}</div> <div style={{ display: "flex", alignItems: "center", gap: 4, marginTop: 4, fontSize: 13, color: "var(--accent-green)" }}> <ArrowUpRight size={14} /> +{portfolio.monthlyChange.percent}% this month </div> </div> <div> <div style={{ fontSize: 12, color: "var(--text-muted)", fontWeight: 500, marginBottom: 4, textTransform: "uppercase", letterSpacing: 1 }}>Liquid Assets</div> <div className="font-display" style={{ fontSize: 24, fontWeight: 600 }}>{formatFullCurrency(portfolio.liquidAssets)}</div> <div style={{ fontSize: 12, color: "var(--text-muted)", marginTop: 4 }}>excl. property & vehicle</div> </div> </div> </div> <div className="card" style={{ padding: 20, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center" }}> <WealthScore score={wellness.overall} size={130} /> <button className="btn-ghost" style={{ marginTop: 8, fontSize: 12, color: "var(--accent-teal)" }} onClick={() => onNavigate("analytics")}>View details →</button> </div> </div> <div className="animate-in-delay-1" style={{ display: "grid", gridTemplateColumns: "1.4fr 1fr", gap: 20 }}> <div className="card" style={{ padding: 24 }}> <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}> <h3 style={{ fontSize: 16, fontWeight: 600 }}>Wealth Trend</h3> <span style={{ fontSize: 12, color: "var(--text-muted)" }}>Last 7 months</span> </div> <ResponsiveContainer width="100%" height={200}> <AreaChart data={portfolio.wealthHistory}> <defs><linearGradient id="wg" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#0EA5E9" stopOpacity={0.25} /><stop offset="100%" stopColor="#0EA5E9" stopOpacity={0} /></linearGradient></defs> <CartesianGrid strokeDasharray="3 3" stroke="rgba(148,163,184,0.08)" /> <XAxis dataKey="month" tick={{ fill: "#64748B", fontSize: 12 }} axisLine={false} tickLine={false} /> <YAxis tick={{ fill: "#64748B", fontSize: 12 }} axisLine={false} tickLine={false} tickFormatter={formatCurrency} domain={['dataMin - 20000', 'dataMax + 10000']} /> <Tooltip content={<CustomTooltip />} /> <Area type="monotone" dataKey="value" stroke="#0EA5E9" strokeWidth={2.5} fill="url(#wg)" dot={{ fill: "#0EA5E9", r: 3, strokeWidth: 0 }} /> </AreaChart> </ResponsiveContainer> </div> <div className="card" style={{ padding: 24 }}> <h3 style={{ fontSize: 16, fontWeight: 600, marginBottom: 8 }}>Asset Allocation</h3> <ResponsiveContainer width="100%" height={180}> <RPieChart> <Pie data={portfolio.allocation} cx="50%" cy="50%" innerRadius={50} outerRadius={80} dataKey="value" stroke="none"> {portfolio.allocation.map((e, i) => <Cell key={i} fill={e.color} />)} </Pie> <Tooltip content={({ active, payload }) => active && payload?.[0] ? ( <div className="tooltip-custom" style={{ padding: "8px 12px" }}> <div style={{ fontSize: 13, fontWeight: 600 }}>{payload[0].name}</div> <div style={{ fontSize: 12, color: "var(--text-secondary)" }}>{formatFullCurrency(payload[0].value)} ({((payload[0].value / portfolio.totalWealth) * 100).toFixed(1)}%)</div> </div> ) : null} /> </RPieChart> </ResponsiveContainer> <div style={{ display: "flex", flexWrap: "wrap", gap: 8, justifyContent: "center" }}> {portfolio.allocation.slice(0, 6).map((a, i) => ( <div key={i} style={{ display: "flex", alignItems: "center", gap: 4, fontSize: 11, color: "var(--text-muted)" }}> <div style={{ width: 8, height: 8, borderRadius: 2, background: a.color }} /> {a.name} </div> ))} </div> </div> </div> {/* AI Insights Preview */} {insights && insights.length > 0 && ( <div className="animate-in-delay-2 card" style={{ padding: 24 }}> <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 16 }}> <div style={{ display: "flex", alignItems: "center", gap: 10 }}> <div style={{ width: 32, height: 32, borderRadius: 10, background: "linear-gradient(135deg, rgba(139,92,246,0.2), rgba(14,165,233,0.2))", display: "flex", alignItems: "center", justifyContent: "center" }}> <Brain size={17} color="var(--accent-purple)" /> </div> <h3 style={{ fontSize: 16, fontWeight: 600 }}>AI Insights</h3> <span className="badge" style={{ background: "rgba(14,165,233,0.12)", color: "var(--accent-teal)", fontSize: 11 }}><Sparkles size={11} /> {insights.length} new</span> </div> <button className="btn-ghost" style={{ fontSize: 13, color: "var(--accent-teal)" }} onClick={() => onNavigate("insights")}>View all →</button> </div> <div style={{ display: "flex", flexDirection: "column", gap: 10 }}> {insights.slice(0, 3).map(ins => ( <div key={ins.id} className={`insight-card ${ins.type}`}> <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 4 }}> {ins.type === "warning" ? <AlertTriangle size={15} color="var(--accent-gold)" /> : ins.type === "opportunity" ? <Lightbulb size={15} color="var(--accent-teal)" /> : <CheckCircle size={15} color="var(--accent-green)" />} <span style={{ fontWeight: 600, fontSize: 14 }}>{ins.title}</span> </div> <p style={{ fontSize: 13, color: "var(--text-secondary)", lineHeight: 1.5 }}>{ins.summary}</p> </div> ))} </div> </div> )} <div className="animate-in-delay-3" style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 12 }}> {[ { icon: <Plus size={18} />, label: "Add Asset", color: "var(--accent-teal)", action: () => onNavigate("wallet") }, { icon: <Target size={18} />, label: "Set Goal", color: "var(--accent-gold)", action: () => onNavigate("goals") }, { icon: <Activity size={18} />, label: "Run Scenario", color: "var(--accent-purple)", action: () => onNavigate("scenarios") }, { icon: <Download size={18} />, label: "Export Report", color: "var(--accent-green)", action: () => onNavigate("export") }, ].map((a, i) => ( <button key={i} className="card" onClick={a.action} style={{ padding: 20, display: "flex", flexDirection: "column", alignItems: "center", gap: 10, cursor: "pointer", border: "1px solid var(--border)", background: "var(--bg-card)" }}> <div style={{ width: 40, height: 40, borderRadius: 12, background: `${a.color}15`, display: "flex", alignItems: "center", justifyContent: "center", color: a.color }}>{a.icon}</div> <span style={{ fontSize: 13, fontWeight: 500, color: "var(--text-primary)" }}>{a.label}</span> </button> ))} </div> </div> ); }; /* ─────────── WALLET VIEW ─────────── */ const WalletView = ({ portfolio, refreshPortfolio }) => { const [filter, setFilter] = useState("All"); const [search, setSearch] = useState(""); const [showAdd, setShowAdd] = useState(false); const [newAsset, setNewAsset] = useState({ name: "", type: "Cash", institution: "", value: "" }); const [saving, setSaving] = useState(false); if (!portfolio) return <Loader />; const types = ["All", ...new Set(portfolio.assets.map(a => a.type))]; const filtered = portfolio.assets .filter(a => filter === "All" || a.type === filter) .filter(a => a.name.toLowerCase().includes(search.toLowerCase()) || a.institution.toLowerCase().includes(search.toLowerCase())); const handleAdd = async () => { if (!newAsset.name || !newAsset.value) return; setSaving(true); try { await api.addAsset(newAsset); await refreshPortfolio(); setShowAdd(false); setNewAsset({ name: "", type: "Cash", institution: "", value: "" }); } catch (err) { console.error(err); } setSaving(false); }; const handleDelete = async (id) => { try { await api.deleteAsset(id); await refreshPortfolio(); } catch (err) { console.error(err); } }; return ( <div style={{ display: "flex", flexDirection: "column", gap: 20 }}> <div className="animate-in" style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}> <div> <h2 className="font-display" style={{ fontSize: 24, fontWeight: 600 }}>Wealth Wallet</h2> <p style={{ color: "var(--text-muted)", fontSize: 14, marginTop: 2 }}>All your assets in one secure view</p> </div> <button className="btn-primary" onClick={() => setShowAdd(!showAdd)} style={{ display: "flex", alignItems: "center", gap: 8, padding: "10px 20px", fontSize: 14 }}> <Plus size={16} /> Add Asset </button> </div> {/* Add Asset Form */} {showAdd && ( <div className="animate-in card" style={{ padding: 24 }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 16 }}>Add New Asset</h3> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr 1fr auto", gap: 12, alignItems: "end" }}> <div> <label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Name *</label> <input className="input" placeholder="e.g. DBS Savings" value={newAsset.name} onChange={e => setNewAsset({...newAsset, name: e.target.value})} /> </div> <div> <label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Type</label> <select className="input" value={newAsset.type} onChange={e => setNewAsset({...newAsset, type: e.target.value})}> {["Cash", "Equities", "Fixed Income", "Crypto", "REITs", "Retirement", "Property", "Vehicle"].map(t => <option key={t} value={t}>{t}</option>)} </select> </div> <div> <label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Institution</label> <input className="input" placeholder="e.g. DBS Bank" value={newAsset.institution} onChange={e => setNewAsset({...newAsset, institution: e.target.value})} /> </div> <div> <label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Value ($) *</label> <input className="input" type="number" placeholder="50000" value={newAsset.value} onChange={e => setNewAsset({...newAsset, value: e.target.value})} /> </div> <button className="btn-primary" onClick={handleAdd} disabled={saving} style={{ padding: "12px 20px" }}> {saving ? <RefreshCw size={16} className="loading-spinner" /> : "Add"} </button> </div> </div> )} {/* Summary */} <div className="animate-in" style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 12 }}> {[ { label: "Total Net Worth", value: formatFullCurrency(portfolio.totalWealth), icon: <Wallet size={18} />, color: "var(--accent-teal)" }, { label: "Monthly Change", value: `+${formatFullCurrency(portfolio.monthlyChange.amount)}`, icon: <TrendingUp size={18} />, color: "var(--accent-green)", sub: `+${portfolio.monthlyChange.percent}%` }, { label: "Assets Tracked", value: portfolio.assets.length, icon: <PieChart size={18} />, color: "var(--accent-purple)", sub: `across ${new Set(portfolio.assets.map(a => a.institution)).size} institutions` }, ].map((c, i) => ( <div key={i} className="card" style={{ padding: 20 }}> <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 12 }}> <div style={{ color: c.color }}>{c.icon}</div> <span style={{ fontSize: 12, color: "var(--text-muted)", textTransform: "uppercase", letterSpacing: 0.5, fontWeight: 500 }}>{c.label}</span> </div> <div className="font-display" style={{ fontSize: 22, fontWeight: 700 }}>{c.value}</div> {c.sub && <div style={{ fontSize: 12, color: "var(--text-muted)", marginTop: 2 }}>{c.sub}</div>} </div> ))} </div> {/* Filters */} <div className="animate-in-delay-1" style={{ display: "flex", gap: 12, alignItems: "center" }}> <div style={{ position: "relative", flex: 1, maxWidth: 280 }}> <Search size={16} style={{ position: "absolute", left: 12, top: "50%", transform: "translateY(-50%)", color: "var(--text-muted)" }} /> <input className="input" value={search} onChange={e => setSearch(e.target.value)} placeholder="Search assets..." style={{ paddingLeft: 36 }} /> </div> <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}> {types.map(t => <button key={t} className="tab-btn" onClick={() => setFilter(t)} style={filter === t ? { background: "var(--accent-teal)", color: "white" } : {}}>{t}</button>)} </div> </div> {/* Assets */} <div className="animate-in-delay-2" style={{ display: "flex", flexDirection: "column", gap: 8 }}> {filtered.map(asset => ( <div key={asset.id} className="card" style={{ padding: "16px 20px", display: "flex", alignItems: "center", justifyContent: "space-between" }}> <div style={{ display: "flex", alignItems: "center", gap: 14 }}> <div style={{ width: 40, height: 40, borderRadius: 12, background: `${asset.color}18`, color: asset.color, display: "flex", alignItems: "center", justifyContent: "center" }}> <AssetIcon type={asset.icon} /> </div> <div> <div style={{ fontWeight: 600, fontSize: 14 }}>{asset.name}</div> <div style={{ fontSize: 12, color: "var(--text-muted)" }}>{asset.institution} · {asset.type}{asset.riskLevel && <span style={{ marginLeft: 6, fontSize: 10, padding: "2px 6px", borderRadius: 4, background: asset.riskScore >= 4 ? "rgba(239,68,68,0.15)" : asset.riskScore >= 3 ? "rgba(245,158,11,0.15)" : "rgba(16,185,129,0.15)", color: asset.riskScore >= 4 ? "var(--accent-red)" : asset.riskScore >= 3 ? "var(--accent-gold)" : "var(--accent-green)", fontWeight: 600 }}>{asset.riskLevel}</span>}</div> </div> </div> <div style={{ display: "flex", alignItems: "center", gap: 16 }}> <div style={{ textAlign: "right" }}> <div style={{ fontWeight: 600, fontSize: 15 }}>{formatFullCurrency(asset.value)}</div> <div style={{ fontSize: 12, fontWeight: 500, color: asset.change >= 0 ? "var(--accent-green)" : "var(--accent-red)", display: "flex", alignItems: "center", gap: 2, justifyContent: "flex-end" }}> {asset.change >= 0 ? <ArrowUpRight size={12} /> : <ArrowDownRight size={12} />}{(Math.abs(asset.change) * 100).toFixed(1)}% </div> </div> <button onClick={() => handleDelete(asset.id)} style={{ background: "none", border: "none", cursor: "pointer", color: "var(--text-muted)", padding: 4, display: "flex" }} title="Delete asset"> <Trash2 size={15} /> </button> </div> </div> ))} </div> </div> ); }; /* ─────────── ANALYTICS VIEW ─────────── */ const AnalyticsView = ({ wellness, portfolio }) => { if (!wellness || !portfolio) return <Loader />; const tips = { "Diversification": "Your portfolio spans multiple asset classes. Consider adding international bonds for better diversification.", "Liquidity": "Good liquid coverage. You could optimize by moving some excess cash to money market funds.", "Growth": "Strong equity exposure with good returns. Your S&P 500 ETF is your top performer.", "Risk Mgmt": "Consider adding more fixed-income assets to reduce overall portfolio volatility.", "Tax Efficiency": "You're under-utilizing SRS and CPF top-ups. Significant tax savings available.", "Emergency Fund": "Well-funded emergency buffer. Consider if some excess cash could work harder elsewhere.", }; const radarData = wellness.metrics.map(m => ({ subject: m.metric, A: m.score, fullMark: 100 })); return ( <div style={{ display: "flex", flexDirection: "column", gap: 20 }}> <div className="animate-in"> <h2 className="font-display" style={{ fontSize: 24, fontWeight: 600 }}>Financial Wellness Analytics</h2> <p style={{ color: "var(--text-muted)", fontSize: 14, marginTop: 2 }}>Deep dive into your financial health metrics</p> </div> <div className="animate-in" style={{ display: "grid", gridTemplateColumns: "1fr 1.2fr", gap: 20 }}> <div className="card" style={{ padding: 28, display: "flex", flexDirection: "column", alignItems: "center" }}> <WealthScore score={wellness.overall} size={180} /> <div style={{ marginTop: 16, textAlign: "center" }}> <div style={{ fontSize: 14, fontWeight: 600, color: wellness.overall >= 75 ? "var(--accent-green)" : "var(--accent-gold)" }}> {wellness.overall >= 75 ? "Excellent" : wellness.overall >= 50 ? "Good – Room to Improve" : "Needs Attention"} </div> <p style={{ fontSize: 13, color: "var(--text-muted)", marginTop: 4, maxWidth: 240 }}>Focus on tax efficiency and risk management to improve your score.</p> </div> </div> <div className="card" style={{ padding: 24 }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 8 }}>Wellness Breakdown</h3> <ResponsiveContainer width="100%" height={240}> <RadarChart data={radarData}> <PolarGrid stroke="rgba(148,163,184,0.15)" /> <PolarAngleAxis dataKey="subject" tick={{ fill: "#94A3B8", fontSize: 11 }} /> <PolarRadiusAxis angle={30} domain={[0, 100]} tick={false} axisLine={false} /> <Radar dataKey="A" stroke="#0EA5E9" fill="#0EA5E9" fillOpacity={0.15} strokeWidth={2} /> </RadarChart> </ResponsiveContainer> </div> </div> <div className="animate-in-delay-1 card" style={{ padding: 24 }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 16 }}>Detailed Metrics</h3> <div style={{ display: "flex", flexDirection: "column", gap: 16 }}> {wellness.metrics.map((m, i) => { const color = m.score >= 75 ? "var(--accent-green)" : m.score >= 50 ? "var(--accent-gold)" : "var(--accent-red)"; const status = m.score >= 75 ? "Excellent" : m.score >= 50 ? "Fair" : "Needs Work"; return ( <div key={i}> <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 6 }}> <span style={{ fontSize: 14, fontWeight: 500 }}>{m.metric}</span> <div style={{ display: "flex", alignItems: "center", gap: 8 }}> <span className="badge" style={{ background: `${color}18`, color, fontSize: 11 }}>{status}</span> <span style={{ fontSize: 14, fontWeight: 700, color }}>{m.score}/100</span> </div> </div> <div style={{ width: "100%", height: 6, borderRadius: 3, background: "rgba(148,163,184,0.1)", marginBottom: 6 }}> <div style={{ width: `${m.score}%`, height: "100%", borderRadius: 3, background: color, transition: "width 1s ease" }} /> </div> <p style={{ fontSize: 12, color: "var(--text-muted)", lineHeight: 1.5 }}>{tips[m.metric] || ""}</p> </div> ); })} </div> </div> </div> ); }; /* ─────────── AI INSIGHTS VIEW ─────────── */ const InsightsView = ({ insights }) => { const [expandedId, setExpandedId] = useState(null); const [aiChatOpen, setAiChatOpen] = useState(false); const [chatMessages, setChatMessages] = useState([ { role: "assistant", text: "Hi Sarah! I'm your AI financial coach. I've analyzed your portfolio and have some observations. What would you like to know?" } ]); const [chatInput, setChatInput] = useState(""); const [chatLoading, setChatLoading] = useState(false); const chatEndRef = useRef(null); useEffect(() => { chatEndRef.current?.scrollIntoView({ behavior: 'smooth' }); }, [chatMessages]); const sendChat = async () => { if (!chatInput.trim()) return; const q = chatInput; setChatMessages(prev => [...prev, { role: "user", text: q }]); setChatInput(""); setChatLoading(true); try { const data = await api.chat(q); setChatMessages(prev => [...prev, { role: "assistant", text: data.reply }]); } catch (err) { setChatMessages(prev => [...prev, { role: "assistant", text: "Sorry, I couldn't process that. Please try again." }]); } setChatLoading(false); }; return ( <div style={{ display: "flex", flexDirection: "column", gap: 20 }}> <div className="animate-in" style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}> <div> <h2 className="font-display" style={{ fontSize: 24, fontWeight: 600 }}>AI Insights & Coach</h2> <p style={{ color: "var(--text-muted)", fontSize: 14, marginTop: 2 }}>Personalized recommendations powered by AI analysis</p> </div> <button className="btn-primary" onClick={() => setAiChatOpen(!aiChatOpen)} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 14, padding: "10px 20px" }}> <MessageSquare size={16} /> {aiChatOpen ? "Close Chat" : "Chat with Coach"} </button> </div> {aiChatOpen && ( <div className="animate-in card" style={{ padding: 0, overflow: "hidden" }}> <div style={{ padding: "14px 20px", background: "linear-gradient(135deg, rgba(139,92,246,0.12), rgba(14,165,233,0.12))", borderBottom: "1px solid var(--border)", display: "flex", alignItems: "center", gap: 10 }}> <Brain size={18} color="var(--accent-purple)" /> <span style={{ fontWeight: 600, fontSize: 14 }}>AI Financial Coach</span> <span className="badge" style={{ background: "rgba(16,185,129,0.15)", color: "var(--accent-green)", fontSize: 10, marginLeft: "auto" }}> <span style={{ width: 6, height: 6, borderRadius: "50%", background: "var(--accent-green)", display: "inline-block" }} /> Online </span> </div> <div className="scrollbar-thin" style={{ height: 280, overflowY: "auto", padding: 20, display: "flex", flexDirection: "column", gap: 16 }}> {chatMessages.map((msg, i) => ( <div key={i} style={{ display: "flex", justifyContent: msg.role === "user" ? "flex-end" : "flex-start" }}> <div style={{ maxWidth: "75%", padding: "12px 16px", borderRadius: 14, fontSize: 14, lineHeight: 1.6, background: msg.role === "user" ? "var(--accent-teal)" : "var(--bg-elevated)", color: msg.role === "user" ? "white" : "var(--text-primary)", borderBottomRightRadius: msg.role === "user" ? 4 : 14, borderBottomLeftRadius: msg.role === "assistant" ? 4 : 14, }}>{msg.text}</div> </div> ))} {chatLoading && ( <div style={{ display: "flex", gap: 6, padding: "12px 16px" }}> {[0, 1, 2].map(i => <div key={i} style={{ width: 8, height: 8, borderRadius: "50%", background: "var(--text-muted)", animation: `pulse 1s ease ${i * 0.2}s infinite` }} />)} </div> )} <div ref={chatEndRef} /> </div> <div style={{ padding: "12px 20px", borderTop: "1px solid var(--border)", display: "flex", gap: 10 }}> <input className="input" value={chatInput} onChange={e => setChatInput(e.target.value)} placeholder="Ask about your finances..." style={{ flex: 1 }} onKeyDown={e => e.key === "Enter" && sendChat()} /> <button className="btn-primary" onClick={sendChat} style={{ padding: "10px 18px" }}><ArrowRight size={18} /></button> </div> </div> )} <div className="animate-in" style={{ display: "flex", gap: 8, flexWrap: "wrap" }}> {["Am I saving enough?", "How to reduce tax?", "Is my portfolio too risky?", "Should I invest more in ETFs?"].map((q, i) => ( <button key={i} className="btn-secondary" style={{ fontSize: 13, padding: "8px 16px" }} onClick={() => { setAiChatOpen(true); setChatInput(q); }}>{q}</button> ))} </div> {insights && ( <div className="animate-in-delay-1" style={{ display: "flex", flexDirection: "column", gap: 10 }}> <h3 style={{ fontSize: 15, fontWeight: 600, color: "var(--text-secondary)" }}>Portfolio Analysis</h3> {insights.map(ins => ( <div key={ins.id} className={`insight-card ${ins.type}`} onClick={() => setExpandedId(expandedId === ins.id ? null : ins.id)}> <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}> <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 4 }}> {ins.type === "warning" ? <AlertTriangle size={15} color="var(--accent-gold)" /> : ins.type === "opportunity" ? <Lightbulb size={15} color="var(--accent-teal)" /> : <CheckCircle size={15} color="var(--accent-green)" />} <span style={{ fontWeight: 600, fontSize: 14 }}>{ins.title}</span> </div> <div style={{ display: "flex", alignItems: "center", gap: 8 }}> <span className="badge" style={{ background: "rgba(148,163,184,0.1)", color: "var(--text-muted)", fontSize: 10 }}>{ins.category}</span> {expandedId === ins.id ? <ChevronUp size={16} color="var(--text-muted)" /> : <ChevronDown size={16} color="var(--text-muted)" />} </div> </div> <p style={{ fontSize: 13, color: "var(--text-secondary)", lineHeight: 1.5 }}>{ins.summary}</p> {expandedId === ins.id && ( <div className="animate-in" style={{ marginTop: 12, padding: "12px 16px", background: "rgba(14,165,233,0.06)", borderRadius: "var(--radius-xs)", border: "1px solid rgba(14,165,233,0.12)" }}> <div style={{ display: "flex", alignItems: "center", gap: 6, marginBottom: 6 }}> <Zap size={13} color="var(--accent-teal)" /> <span style={{ fontSize: 12, fontWeight: 600, color: "var(--accent-teal)", textTransform: "uppercase", letterSpacing: 0.5 }}>Suggested Action</span> </div> <p style={{ fontSize: 13, color: "var(--text-primary)", lineHeight: 1.5 }}>{ins.action}</p> </div> )} </div> ))} </div> )} </div> ); }; /* ─────────── SCENARIO VIEW ─────────── */ const ScenarioView = () => { const [selected, setSelected] = useState(null); const [custom, setCustom] = useState({ equity: 0, crypto: 0, property: 0, cash: 0 }); const [result, setResult] = useState(null); const [simulating, setSimulating] = useState(false); const presets = [ { name: "Market Crash (-30%)", eq: -0.3, cr: -0.5, pr: -0.1, ca: 0 }, { name: "Rate Hike (+2%)", eq: -0.1, cr: -0.15, pr: -0.05, ca: 0.02 }, { name: "Bull Market (+20%)", eq: 0.2, cr: 0.4, pr: 0.05, ca: 0.01 }, { name: "Recession", eq: -0.2, cr: -0.35, pr: -0.08, ca: 0 }, ]; const runSimulation = async (params) => { setSimulating(true); try { const data = await api.simulate(params); setResult(data); } catch (err) { console.error(err); } setSimulating(false); }; useEffect(() => { const params = selected !== null ? { equityChange: presets[selected].eq, cryptoChange: presets[selected].cr, propertyChange: presets[selected].pr, cashChange: presets[selected].ca } : { equityChange: custom.equity / 100, cryptoChange: custom.crypto / 100, propertyChange: custom.property / 100, cashChange: custom.cash / 100 }; runSimulation(params); }, [selected, custom]); return ( <div style={{ display: "flex", flexDirection: "column", gap: 20 }}> <div className="animate-in"> <h2 className="font-display" style={{ fontSize: 24, fontWeight: 600 }}>Scenario Simulator</h2> <p style={{ color: "var(--text-muted)", fontSize: 14, marginTop: 2 }}>Test how market events could affect your wealth</p> </div> <div className="animate-in" style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 10 }}> {presets.map((s, i) => ( <button key={i} onClick={() => setSelected(selected === i ? null : i)} className="card" style={{ padding: 16, cursor: "pointer", textAlign: "center", borderColor: selected === i ? "var(--accent-teal)" : "var(--border)", background: selected === i ? "rgba(14,165,233,0.06)" : "var(--bg-card)" }}> <div style={{ fontSize: 13, fontWeight: 600, marginBottom: 4, color: "var(--text-primary)" }}>{s.name}</div> <div style={{ fontSize: 11, color: "var(--text-muted)" }}>Equity: {s.eq > 0 ? "+" : ""}{(s.eq * 100).toFixed(0)}%</div> </button> ))} </div> <div className="animate-in-delay-1 card" style={{ padding: 24 }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 16 }}>Custom Scenario</h3> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}> {[{ label: "Equities", key: "equity" }, { label: "Crypto", key: "crypto" }, { label: "Property", key: "property" }, { label: "Cash/Bonds", key: "cash" }].map(s => ( <div key={s.key}> <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 6 }}> <span style={{ fontSize: 13, fontWeight: 500 }}>{s.label}</span> <span style={{ fontSize: 13, fontWeight: 600, color: custom[s.key] >= 0 ? "var(--accent-green)" : "var(--accent-red)" }}>{custom[s.key] > 0 ? "+" : ""}{custom[s.key]}%</span> </div> <input type="range" min={-50} max={50} value={custom[s.key]} onChange={e => { setSelected(null); setCustom(prev => ({ ...prev, [s.key]: parseInt(e.target.value) })); }} style={{ width: "100%", accentColor: "var(--accent-teal)" }} /> </div> ))} </div> </div> {result && ( <div className="animate-in-delay-2" style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 20 }}> <div className="card" style={{ padding: 24, textAlign: "center" }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 16 }}>Impact Summary</h3> {simulating ? <Loader text="Simulating..." /> : ( <> <div style={{ fontSize: 12, color: "var(--text-muted)", marginBottom: 4 }}>Projected Net Worth</div> <div className="font-display" style={{ fontSize: 28, fontWeight: 700 }}>{formatFullCurrency(result.projectedTotal)}</div> <div style={{ fontSize: 15, fontWeight: 600, marginTop: 4, color: result.change >= 0 ? "var(--accent-green)" : "var(--accent-red)" }}> {result.change >= 0 ? "+" : ""}{formatFullCurrency(result.change)} ({result.changePercent.toFixed(1)}%) </div> </> )} </div> <div className="card" style={{ padding: 24 }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 12 }}>Asset Impact Breakdown</h3> <div className="scrollbar-thin" style={{ maxHeight: 200, overflowY: "auto", display: "flex", flexDirection: "column", gap: 8 }}> {result.breakdown.filter(a => a.change !== 0).map((a, i) => ( <div key={i} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", fontSize: 13 }}> <div style={{ display: "flex", alignItems: "center", gap: 6 }}> <div style={{ width: 8, height: 8, borderRadius: 2, background: a.color }} /><span>{a.name}</span> </div> <span style={{ fontWeight: 600, color: a.change >= 0 ? "var(--accent-green)" : "var(--accent-red)" }}> {a.change >= 0 ? "+" : ""}{formatFullCurrency(a.change)} </span> </div> ))} </div> </div> </div> )} </div> ); }; 
/* ─────────── ACCOUNT SETTINGS ─────────── */
const AccountSettings = () => { const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [curPw, setCurPw] = useState(""); const [newPw, setNewPw] = useState(""); const [saving, setSaving] = useState(false); const [msg, setMsg] = useState(""); const [err, setErr] = useState(""); useEffect(() => { api.getProfile().then(p => { setName(p.name); setEmail(p.email); }).catch(() => {}); }, []); const handleSave = async () => { setSaving(true); setMsg(""); setErr(""); try { const body = { name, email }; if (newPw) { body.currentPassword = curPw; body.newPassword = newPw; } await api.updateProfile(body); setMsg("Profile updated"); setCurPw(""); setNewPw(""); } catch (e) { setErr(e.message); } setSaving(false); }; return ( <div className="animate-in card" style={{ padding: 28 }}> <h3 style={{ fontSize: 16, fontWeight: 600, marginBottom: 20 }}>Account Information</h3> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}> <div><label style={{ fontSize: 13, color: "var(--text-muted)", fontWeight: 500, display: "block", marginBottom: 6 }}>Name</label><input className="input" value={name} onChange={e => setName(e.target.value)} /></div> <div><label style={{ fontSize: 13, color: "var(--text-muted)", fontWeight: 500, display: "block", marginBottom: 6 }}>Email</label><input className="input" value={email} onChange={e => setEmail(e.target.value)} /></div> </div> <div style={{ marginTop: 20, padding: 16, background: "var(--bg-secondary)", borderRadius: "var(--radius-sm)" }}> <h4 style={{ fontSize: 14, fontWeight: 600, marginBottom: 12, color: "var(--text-primary)" }}>Change Password</h4> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12 }}> <div><label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Current Password</label><input className="input" type="password" value={curPw} onChange={e => setCurPw(e.target.value)} placeholder="Current password" /></div> <div><label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>New Password</label><input className="input" type="password" value={newPw} onChange={e => setNewPw(e.target.value)} placeholder="New password" /></div> </div> </div> <div style={{ display: "flex", alignItems: "center", gap: 12, marginTop: 16 }}> <button className="btn-primary" onClick={handleSave} disabled={saving} style={{ padding: "10px 24px", fontSize: 14 }}>{saving ? "Saving..." : "Save Changes"}</button> {msg && <span style={{ fontSize: 13, color: "var(--accent-green)", display: "flex", alignItems: "center", gap: 4 }}><CheckCircle size={14} /> {msg}</span>} {err && <span style={{ fontSize: 13, color: "var(--accent-red)", display: "flex", alignItems: "center", gap: 4 }}><AlertCircle size={14} /> {err}</span>} </div> </div> ); };

/* ─────────── GOALS VIEW ─────────── */
const GoalsView = () => { const [goals, setGoals] = useState([]); const [loading, setLoading] = useState(true); const [showAdd, setShowAdd] = useState(false); const [newGoal, setNewGoal] = useState({ title: "", description: "", targetAmount: "", deadline: "", category: "general" }); const [saving, setSaving] = useState(false); const loadGoals = async () => { try { const data = await api.getGoals(); setGoals(data.goals || []); } catch (e) { console.error(e); } setLoading(false); }; useEffect(() => { loadGoals(); }, []); const handleAdd = async () => { if (!newGoal.title || !newGoal.targetAmount) return; setSaving(true); try { await api.createGoal(newGoal); await loadGoals(); setShowAdd(false); setNewGoal({ title: "", description: "", targetAmount: "", deadline: "", category: "general" }); } catch (e) { console.error(e); } setSaving(false); }; const handleDelete = async (id) => { try { await api.deleteGoal(id); await loadGoals(); } catch (e) { console.error(e); } }; const catColors = { safety: "var(--accent-green)", tax: "var(--accent-teal)", growth: "var(--accent-purple)", general: "var(--accent-gold)" }; const catIcons = { safety: <ShieldCheck size={18} />, tax: <DollarSign size={18} />, growth: <TrendingUp size={18} />, general: <Target size={18} /> }; if (loading) return <Loader text="Loading goals..." />; return ( <div style={{ display: "flex", flexDirection: "column", gap: 20 }}> <div className="animate-in" style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}> <div> <h2 className="font-display" style={{ fontSize: 24, fontWeight: 600 }}>Financial Goals</h2> <p style={{ color: "var(--text-muted)", fontSize: 14, marginTop: 2 }}>Track progress towards your financial milestones</p> </div> <button className="btn-primary" onClick={() => setShowAdd(!showAdd)} style={{ display: "flex", alignItems: "center", gap: 8, padding: "10px 20px", fontSize: 14 }}><Plus size={16} /> New Goal</button> </div> {showAdd && ( <div className="animate-in card" style={{ padding: 24 }}> <h3 style={{ fontSize: 15, fontWeight: 600, marginBottom: 16 }}>Create New Goal</h3> <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12 }}> <div><label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Goal Title *</label><input className="input" placeholder="e.g. Emergency Fund" value={newGoal.title} onChange={e => setNewGoal({...newGoal, title: e.target.value})} /></div> <div><label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Target Amount ($) *</label><input className="input" type="number" placeholder="50000" value={newGoal.targetAmount} onChange={e => setNewGoal({...newGoal, targetAmount: e.target.value})} /></div> <div><label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Category</label><select className="input" value={newGoal.category} onChange={e => setNewGoal({...newGoal, category: e.target.value})}><option value="safety">Safety Net</option><option value="tax">Tax Optimization</option><option value="growth">Growth</option><option value="general">General</option></select></div> <div><label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Target Date</label><input className="input" type="date" value={newGoal.deadline} onChange={e => setNewGoal({...newGoal, deadline: e.target.value})} /></div> <div style={{ gridColumn: "1 / -1" }}><label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Description</label><input className="input" placeholder="What is this goal about?" value={newGoal.description} onChange={e => setNewGoal({...newGoal, description: e.target.value})} /></div> </div> <div style={{ display: "flex", gap: 10, marginTop: 16 }}><button className="btn-primary" onClick={handleAdd} disabled={saving}>{saving ? "Saving..." : "Create Goal"}</button><button className="btn-secondary" onClick={() => setShowAdd(false)}>Cancel</button></div> </div> )} {goals.length === 0 ? ( <div className="card" style={{ padding: 40, textAlign: "center" }}><Target size={40} color="var(--text-muted)" style={{ marginBottom: 12 }} /><h3 style={{ fontSize: 16, fontWeight: 600, marginBottom: 4 }}>No goals yet</h3><p style={{ color: "var(--text-muted)", fontSize: 14 }}>Set your first financial goal to start tracking</p></div> ) : ( <div className="animate-in" style={{ display: "flex", flexDirection: "column", gap: 12 }}> {goals.map(g => { const color = catColors[g.category] || "var(--accent-gold)"; return ( <div key={g.id} className="card" style={{ padding: 20 }}> <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", marginBottom: 12 }}> <div style={{ display: "flex", alignItems: "center", gap: 10 }}> <div style={{ width: 36, height: 36, borderRadius: 10, background: color + "18", color, display: "flex", alignItems: "center", justifyContent: "center" }}>{catIcons[g.category] || <Target size={18} />}</div> <div><div style={{ fontWeight: 600, fontSize: 15, color: "var(--text-primary)" }}>{g.title}</div>{g.description && <div style={{ fontSize: 12, color: "var(--text-muted)", marginTop: 1 }}>{g.description}</div>}</div> </div> <div style={{ display: "flex", alignItems: "center", gap: 8 }}> {g.isComplete && <span className="badge" style={{ background: "rgba(16,185,129,0.15)", color: "var(--accent-green)", fontSize: 11 }}><CheckCircle size={11} /> Complete</span>} {g.daysLeft > 0 && !g.isComplete && <span style={{ fontSize: 12, color: "var(--text-muted)" }}>{g.daysLeft}d left</span>} <button onClick={() => handleDelete(g.id)} style={{ background: "none", border: "none", cursor: "pointer", color: "var(--text-muted)", padding: 4, display: "flex" }}><Trash2 size={14} /></button> </div> </div> <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13, marginBottom: 8 }}> <span style={{ color: "var(--text-secondary)" }}>{formatFullCurrency(g.currentAmount)} of {formatFullCurrency(g.targetAmount)}</span> <span style={{ fontWeight: 600, color }}>{g.progress}%</span> </div> <div style={{ width: "100%", height: 8, borderRadius: 4, background: "rgba(148,163,184,0.1)" }}><div style={{ width: g.progress + "%", height: "100%", borderRadius: 4, background: g.isComplete ? "var(--accent-green)" : color, transition: "width 0.8s ease" }} /></div> {g.remaining > 0 && <div style={{ fontSize: 12, color: "var(--text-muted)", marginTop: 6 }}>{formatFullCurrency(g.remaining)} remaining</div>} </div> ); })} </div> )} </div> ); };

/* ─────────── EXPORT & EMAIL VIEW ─────────── */
const ExportView = () => { const [exporting, setExporting] = useState(false); const [emailPrefs, setEmailPrefs] = useState(null); const [loadingPrefs, setLoadingPrefs] = useState(true); const [sending, setSending] = useState(false); const [sentMsg, setSentMsg] = useState(""); const [customEmail, setCustomEmail] = useState(""); useEffect(() => { api.getEmailPrefs().then(p => { setEmailPrefs(p); setCustomEmail(p.email || ""); setLoadingPrefs(false); }).catch(() => setLoadingPrefs(false)); }, []); const handleExport = async (format) => { setExporting(true); try { const url = "/api/export/" + format; const res = await fetch(url, { headers: { "Authorization": "Bearer " + api.token } }); const blob = await res.blob(); const a = document.createElement("a"); a.href = URL.createObjectURL(blob); a.download = "wealthwell-report." + (format === "json" ? "json" : "csv"); document.body.appendChild(a); a.click(); document.body.removeChild(a); URL.revokeObjectURL(a.href); } catch (e) { console.error(e); } setExporting(false); }; const handleFreqChange = async (freq) => { try { const updated = await api.updateEmailPrefs({ reportFrequency: freq }); setEmailPrefs(updated); } catch (e) { console.error(e); } }; const handleSendNow = async () => { if (!customEmail) return; setSending(true); setSentMsg(""); try { const data = await api.sendReport(customEmail); setSentMsg(data.message); const updated = await api.getEmailPrefs(); setEmailPrefs(updated); } catch (e) { setSentMsg("Failed to send"); } setSending(false); }; return ( <div style={{ display: "flex", flexDirection: "column", gap: 20 }}> <div className="animate-in"> <h2 className="font-display" style={{ fontSize: 24, fontWeight: 600 }}>Export & Reports</h2> <p style={{ color: "var(--text-muted)", fontSize: 14, marginTop: 2 }}>Download your data or schedule email reports</p> </div> <div className="animate-in" style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}> <div className="card" style={{ padding: 28, textAlign: "center" }}> <div style={{ width: 48, height: 48, borderRadius: 14, background: "rgba(16,185,129,0.12)", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 12px" }}><FileText size={24} color="var(--accent-green)" /></div> <h3 style={{ fontSize: 16, fontWeight: 600, marginBottom: 4 }}>Export as CSV</h3> <p style={{ fontSize: 13, color: "var(--text-muted)" }}>Open in Excel or Google Sheets</p> <button className="btn-primary" onClick={() => handleExport("csv")} disabled={exporting} style={{ marginTop: 16, padding: "10px 24px", fontSize: 14 }}>{exporting ? "Exporting..." : "Download CSV"}</button> </div> <div className="card" style={{ padding: 28, textAlign: "center" }}> <div style={{ width: 48, height: 48, borderRadius: 14, background: "rgba(14,165,233,0.12)", display: "flex", alignItems: "center", justifyContent: "center", margin: "0 auto 12px" }}><Download size={24} color="var(--accent-teal)" /></div> <h3 style={{ fontSize: 16, fontWeight: 600, marginBottom: 4 }}>Export as JSON</h3> <p style={{ fontSize: 13, color: "var(--text-muted)" }}>Full data with insights and goals</p> <button className="btn-secondary" onClick={() => handleExport("json")} disabled={exporting} style={{ marginTop: 16, padding: "10px 24px", fontSize: 14 }}>{exporting ? "Exporting..." : "Download JSON"}</button> </div> </div> <div className="animate-in-delay-1 card" style={{ padding: 28 }}> <h3 style={{ fontSize: 16, fontWeight: 600, marginBottom: 4 }}>Email Reports</h3> <p style={{ color: "var(--text-muted)", fontSize: 13, marginBottom: 20 }}>Receive a summary report delivered to your inbox</p> {loadingPrefs ? <Loader text="Loading..." /> : ( <> <div style={{ display: "flex", gap: 8, marginBottom: 20 }}> {[{ key: "monthly", label: "Monthly" }, { key: "yearly", label: "Yearly" }, { key: "off", label: "Off" }].map(opt => ( <button key={opt.key} className="tab-btn" onClick={() => handleFreqChange(opt.key)} style={emailPrefs?.reportFrequency === opt.key ? { background: "var(--accent-teal)", color: "white" } : {}}>{opt.label}</button> ))} </div> {emailPrefs?.reportFrequency !== "off" && ( <div style={{ padding: 16, background: "var(--bg-secondary)", borderRadius: "var(--radius-sm)", marginBottom: 16 }}> <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13 }}><span style={{ color: "var(--text-muted)" }}>Frequency</span><span style={{ fontWeight: 500, color: "var(--text-primary)" }}>{emailPrefs?.reportFrequency}</span></div> {emailPrefs?.nextScheduled && <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13, marginTop: 8 }}><span style={{ color: "var(--text-muted)" }}>Next report</span><span style={{ fontWeight: 500, color: "var(--text-primary)" }}>{emailPrefs.nextScheduled}</span></div>} {emailPrefs?.lastSent && <div style={{ display: "flex", justifyContent: "space-between", fontSize: 13, marginTop: 8 }}><span style={{ color: "var(--text-muted)" }}>Last sent</span><span style={{ fontWeight: 500, color: "var(--text-primary)" }}>{emailPrefs.lastSent}</span></div>} </div> )} <div style={{ display: "flex", gap: 10, alignItems: "end" }}> <div style={{ flex: 1 }}><label style={{ fontSize: 12, color: "var(--text-muted)", display: "block", marginBottom: 4 }}>Send report to</label><input className="input" value={customEmail} onChange={e => setCustomEmail(e.target.value)} placeholder="Enter email address" style={{ fontSize: 13 }} /></div> <button className="btn-primary" onClick={handleSendNow} disabled={sending || !customEmail} style={{ padding: "12px 20px", fontSize: 14, display: "flex", alignItems: "center", gap: 8, whiteSpace: "nowrap" }}>{sending ? <RefreshCw size={16} className="loading-spinner" /> : <Bell size={16} />} Send Now</button> </div> {sentMsg && <div style={{ marginTop: 10, fontSize: 13, color: "var(--accent-green)", display: "flex", alignItems: "center", gap: 4 }}><CheckCircle size={14} /> {sentMsg}</div>} </> )} </div> </div> ); };

/* ─────────── SETTINGS VIEW ─────────── */ const SettingsView = () => { const [activeTab, setActiveTab] = useState("account"); const [upgrading, setUpgrading] = useState(false); const handleUpgrade = async (plan) => { setUpgrading(true); try { const data = await api.createCheckout(plan); alert(`Stripe Checkout: ${data.message}\n\nPlan: ${plan}\nAmount: $${data.amount} SGD/month\nSession: ${data.sessionId}`); } catch (err) { console.error(err); } setUpgrading(false); }; return ( <div style={{ display: "flex", flexDirection: "column", gap: 20 }}> <div className="animate-in"><h2 className="font-display" style={{ fontSize: 24, fontWeight: 600 }}>Settings</h2></div> <div className="animate-in" style={{ display: "flex", gap: 6 }}> {[{ key: "account", label: "Account" }, { key: "security", label: "Security" }, { key: "plan", label: "Subscription" }].map(t => ( <button key={t.key} className="tab-btn" onClick={() => setActiveTab(t.key)} style={activeTab === t.key ? { background: "var(--accent-teal)", color: "white" } : {}}>{t.label}</button> ))} </div> {activeTab === "account" && ( <AccountSettings /> )} {activeTab === "security" && ( <div className="animate-in card" style={{ padding: 28 }}> <h3 style={{ fontSize: 16, fontWeight: 600, marginBottom: 20 }}>Security Settings</h3> <div style={{ display: "flex", flexDirection: "column", gap: 16 }}> {[ { icon: <ShieldCheck size={20} color="var(--accent-green)" />, title: "Two-Factor Authentication", sub: "Enabled (Microsoft Authenticator)", btn: "Configure" }, { icon: <Key size={20} color="var(--accent-teal)" />, title: "Password", sub: "Last changed 45 days ago", btn: "Change" }, { icon: <Eye size={20} color="var(--accent-purple)" />, title: "Data Access", sub: "Read-only connections · AES-256 encryption", btn: "View Audit Log" }, { icon: <Smartphone size={20} color="var(--accent-gold)" />, title: "Active Sessions", sub: "2 devices active", btn: "Manage" }, ].map((item, i) => ( <div key={i} style={{ padding: 16, background: "var(--bg-secondary)", borderRadius: "var(--radius-sm)", display: "flex", alignItems: "center", justifyContent: "space-between" }}> <div style={{ display: "flex", alignItems: "center", gap: 12 }}> {item.icon} <div><div style={{ fontSize: 14, fontWeight: 500 }}>{item.title}</div><div style={{ fontSize: 12, color: "var(--text-muted)" }}>{item.sub}</div></div> </div> <button className="btn-secondary" style={{ fontSize: 13 }}>{item.btn}</button> </div> ))} </div> </div> )} {activeTab === "plan" && ( <div className="animate-in" style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 16 }}> <div className="tier-card" style={{ background: "var(--bg-card)" }}> <h3 style={{ fontSize: 18, fontWeight: 700, marginBottom: 4 }}>Lite</h3> <p style={{ fontSize: 13, color: "var(--text-muted)", marginBottom: 20 }}>Get started for free</p> <div className="font-display" style={{ fontSize: 36, fontWeight: 700, marginBottom: 24 }}>$0<span style={{ fontSize: 14, fontWeight: 400, color: "var(--text-muted)" }}>/month</span></div> <div style={{ display: "flex", flexDirection: "column", gap: 10, marginBottom: 28 }}> {["Up to 5 accounts", "Basic dashboard", "Monthly wellness score", "Standard encryption"].map((f, i) => <div key={i} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13, color: "var(--text-secondary)" }}><CheckCircle size={14} color="var(--accent-green)" /> {f}</div>)} {["AI insights & coach", "Scenario simulator", "Export reports"].map((f, i) => <div key={i} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13, color: "var(--text-muted)" }}><X size={14} /> {f}</div>)} </div> <button className="btn-secondary" style={{ width: "100%" }}>Free Plan</button> </div> <div className="tier-card recommended"> <div style={{ position: "absolute", top: 12, right: 12, padding: "4px 12px", borderRadius: 20, background: "linear-gradient(135deg, #0EA5E9, #6366F1)", fontSize: 11, fontWeight: 600, color: "white" }}>RECOMMENDED</div> <h3 style={{ fontSize: 18, fontWeight: 700, marginBottom: 4 }}>Pro</h3> <p style={{ fontSize: 13, color: "var(--text-muted)", marginBottom: 20 }}>For serious wealth builders</p> <div className="font-display" style={{ fontSize: 36, fontWeight: 700, marginBottom: 24 }}>$9.90<span style={{ fontSize: 14, fontWeight: 400, color: "var(--text-muted)" }}>/month</span></div> <div style={{ display: "flex", flexDirection: "column", gap: 10, marginBottom: 28 }}> {["Unlimited accounts", "Full dashboard", "Real-time wellness score", "AI insights & coach", "Scenario simulator", "Monthly PDF reports", "Bank-grade encryption"].map((f, i) => <div key={i} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13, color: "var(--text-secondary)" }}><CheckCircle size={14} color="var(--accent-teal)" /> {f}</div>)} </div> <button className="btn-primary" onClick={() => handleUpgrade('pro')} disabled={upgrading} style={{ width: "100%", display: "flex", alignItems: "center", justifyContent: "center", gap: 8 }}> <CreditCard size={16} /> Upgrade to Pro </button> <p style={{ textAlign: "center", fontSize: 11, color: "var(--text-muted)", marginTop: 8 }}>Powered by Stripe · Cancel anytime</p> </div> <div className="tier-card" style={{ background: "var(--bg-card)" }}> <h3 style={{ fontSize: 18, fontWeight: 700, marginBottom: 4 }}>Family</h3> <p style={{ fontSize: 13, color: "var(--text-muted)", marginBottom: 20 }}>Manage household wealth</p> <div className="font-display" style={{ fontSize: 36, fontWeight: 700, marginBottom: 24 }}>$19.90<span style={{ fontSize: 14, fontWeight: 400, color: "var(--text-muted)" }}>/month</span></div> <div style={{ display: "flex", flexDirection: "column", gap: 10, marginBottom: 28 }}> {["Everything in Pro", "Up to 4 family members", "Household wealth view", "Joint financial goals", "Family scenario planning", "Dedicated advisor access", "Priority support"].map((f, i) => <div key={i} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13, color: "var(--text-secondary)" }}><CheckCircle size={14} color="var(--accent-purple)" /> {f}</div>)} </div> <button className="btn-secondary" onClick={() => handleUpgrade('family')} disabled={upgrading} style={{ width: "100%" }}>Choose Family</button> </div> </div> )} </div> ); }; /* ─────────── MAIN APP ─────────── */ const App = () => { const [appState, setAppState] = useState("login"); const [currentView, setCurrentView] = useState("dashboard"); const [sidebarCollapsed, setSidebarCollapsed] = useState(false); const [user, setUser] = useState(null); const [portfolio, setPortfolio] = useState(null); const [wellness, setWellness] = useState(null); const [insights, setInsights] = useState(null); const loadData = useCallback(async () => { try { const [p, w, i] = await Promise.all([api.getPortfolio(), api.getWellness(), api.getInsights()]); setPortfolio(p); setWellness(w); setInsights(i.insights); } catch (err) { console.error("Failed to load data:", err); } }, []); const refreshPortfolio = useCallback(async () => { try { const [p, w, i] = await Promise.all([api.getPortfolio(), api.getWellness(), api.getInsights()]); setPortfolio(p); setWellness(w); setInsights(i.insights); } catch (err) { console.error(err); } }, []); const handleLogin = (userData) => { setUser(userData); setAppState("onboarding"); }; const handleOnboardingComplete = () => { setAppState("app"); loadData(); }; const handleLogout = async () => {     try {       await api.logout();     } catch (e) {   }      api.token = null;      setUser(null);     setPortfolio(null);     setWellness(null);     setInsights(null);      setAppState("login");     setCurrentView("login"); };    const navItems = [     { key: "dashboard", label: "Dashboard", icon: <BarChart3 size={19} /> },     { key: "wallet", label: "Wealth Wallet", icon: <Wallet size={19} /> },     { key: "analytics", label: "Analytics", icon: <Activity size={19} /> },     { key: "insights", label: "AI Insights", icon: <Brain size={19} /> },     { key: "goals", label: "Goals", icon: <Target size={19} /> },     { key: "scenarios", label: "Scenarios", icon: <Activity size={19} /> },     { key: "export", label: "Export", icon: <Download size={19} /> },     { key: "settings", label: "Settings", icon: <Settings size={19} /> },   ];    if (appState === "login") return (<><link href={FONTS_LINK} rel="stylesheet" /><style>{CSS}</style><LoginPage onLogin={handleLogin} /></>);   if (appState === "onboarding") return (<><link href={FONTS_LINK} rel="stylesheet" /><style>{CSS}</style><OnboardingPage onComplete={handleOnboardingComplete} /></>);    const renderView = () => {     switch (currentView) {       case "dashboard": return <DashboardView portfolio={portfolio} wellness={wellness} insights={insights} onNavigate={setCurrentView} />;       case "wallet": return <WalletView portfolio={portfolio} refreshPortfolio={refreshPortfolio} />;       case "analytics": return <AnalyticsView portfolio={portfolio} wellness={wellness} />;       case "insights": return <InsightsView insights={insights} />;       case "goals": return <GoalsView />;       case "scenarios": return <ScenarioView />;       case "export": return <ExportView />;       case "settings": return <SettingsView user={user} setUser={setUser} />;       default: return <DashboardView portfolio={portfolio} wellness={wellness} insights={insights} onNavigate={setCurrentView} />;     }   };    return (     <><link href={FONTS_LINK} rel="stylesheet" /><style>{CSS}</style>     <div style={{ display: "flex", height: "100vh", overflow: "hidden", background: "var(--bg-primary)" }}>       <aside style={{ width: sidebarCollapsed ? 72 : 240, background: "var(--bg-secondary)", borderRight: "1px solid var(--border)", display: "flex", flexDirection: "column", transition: "width 0.25s ease", flexShrink: 0 }}>         <div style={{ padding: sidebarCollapsed ? "20px 12px" : "20px 20px", display: "flex", alignItems: "center", gap: 10, borderBottom: "1px solid var(--border)" }}>           <div style={{ width: 36, height: 36, borderRadius: 10, flexShrink: 0, background: "linear-gradient(135deg, #0EA5E9, #6366F1)", display: "flex", alignItems: "center", justifyContent: "center" }}><Wallet size={20} color="white" strokeWidth={1.8} /></div>           {!sidebarCollapsed && <span className="font-display" style={{ fontSize: 18, fontWeight: 600, whiteSpace: "nowrap" }}>WealthWell</span>}         </div>         <nav style={{ flex: 1, padding: "12px 8px", display: "flex", flexDirection: "column", gap: 2 }}>           {navItems.map(item => (             <button key={item.key} className={`sidebar-item ${currentView === item.key ? "active" : ""}`} onClick={() => setCurrentView(item.key)} style={sidebarCollapsed ? { justifyContent: "center", padding: "12px" } : {}}>               {item.icon}{!sidebarCollapsed && <span>{item.label}</span>}             </button>           ))}         </nav>         <div style={{ padding: sidebarCollapsed ? "16px 8px" : "16px 12px", borderTop: "1px solid var(--border)" }}>           {!sidebarCollapsed ? (             <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>               <div style={{ display: "flex", alignItems: "center", gap: 10, cursor: "pointer" }} onClick={() => setCurrentView("settings")}>                 <div style={{ width: 34, height: 34, borderRadius: 10, flexShrink: 0, background: "linear-gradient(135deg, #6366F1, #EC4899)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 14, fontWeight: 600, color: "white" }}>{user?.name?.charAt(0) || "U"}</div>                 <div style={{ overflow: "hidden" }}>                   <div style={{ fontSize: 13, fontWeight: 600, whiteSpace: "nowrap" }}>{user?.name || "User"}</div>                   <div style={{ fontSize: 11, color: "var(--text-muted)", display: "flex", alignItems: "center", gap: 4 }}><Crown size={10} color="var(--accent-gold)" /> {user?.plan === "pro" ? "Pro" : "Lite"} Plan</div>                 </div>               </div>               <button onClick={handleLogout} style={{ background: "none", border: "none", cursor: "pointer", color: "var(--text-muted)", padding: 4, display: "flex" }}><LogOut size={16} /></button>             </div>           ) : (             <div style={{ display: "flex", justifyContent: "center" }}>               <div style={{ width: 34, height: 34, borderRadius: 10, background: "linear-gradient(135deg, #6366F1, #EC4899)", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 14, fontWeight: 600, color: "white" }}>{user?.name?.charAt(0) || "U"}</div>             </div>           )}         </div>       </aside>       <main style={{ flex: 1, overflow: "hidden", display: "flex", flexDirection: "column" }}>         <header style={{ padding: "14px 28px", borderBottom: "1px solid var(--border)", display: "flex", alignItems: "center", justifyContent: "space-between", background: "var(--bg-secondary)" }}>           <div style={{ display: "flex", alignItems: "center", gap: 12 }}>             <button onClick={() => setSidebarCollapsed(!sidebarCollapsed)} style={{ background: "none", border: "none", cursor: "pointer", color: "var(--text-muted)", display: "flex", padding: 4 }}><Menu size={20} /></button>             <span style={{ fontSize: 15, fontWeight: 600 }}>{navItems.find(n => n.key === currentView)?.label}</span>           </div>           <div style={{ display: "flex", alignItems: "center", gap: 12 }}>             <div style={{ position: "relative" }}><button style={{ background: "none", border: "none", cursor: "pointer", color: "var(--text-muted)", display: "flex", padding: 6 }}><Bell size={18} /></button><div style={{ position: "absolute", top: 4, right: 4, width: 7, height: 7, borderRadius: "50%", background: "var(--accent-red)" }} /></div>             <button style={{ background: "none", border: "none", cursor: "pointer", color: "var(--text-muted)", display: "flex", padding: 6 }}><HelpCircle size={18} /></button>           </div>         </header>         <div className="scrollbar-thin" style={{ flex: 1, overflow: "auto", padding: 28 }}>           <div key={currentView} style={{ maxWidth: 1000 }}>             {portfolio ? renderView() : <div style={{ display: "flex", alignItems: "center", justifyContent: "center", height: "60vh" }}><div style={{ textAlign: "center" }}><RefreshCw size={32} color="var(--accent-teal)" className="loading-spinner" style={{ marginBottom: 16 }} /><p style={{ color: "var(--text-muted)" }}>Loading your financial data...</p></div></div>}           </div>         </div>       </main>     </div>     </>   ); };  export default App;
